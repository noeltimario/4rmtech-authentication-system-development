import { auth } from '@/lib/auth'
import { headers } from 'next/headers'
import { redirect } from 'next/navigation'

export default async function Dashboard(){const s=await auth.api.getSession({headers:await headers()}); if(!s?.user) redirect('/login'); const role=(s.user as {role?:string}).role||'staff'; return <section><div className="page-heading"><div><p className="eyebrow">COMMAND CENTER / 00{role==='admin'?'1':'2'}</p><h1>Operations overview</h1><p>Track the signals that keep your shop moving.</p></div><a href={role==='admin'?'/admin/dashboard':'/staff/dashboard'} className="primary-btn compact">Open workspace</a></div><div className="metric-grid"><article className="metric-card accent"><span>Revenue this month</span><strong>₱248,690</strong><small>↑ 18.4% vs last month</small></article><article className="metric-card"><span>Orders in progress</span><strong>28</strong><small>7 need attention</small></article><article className="metric-card"><span>Active catalog</span><strong>142</strong><small>12 low-stock items</small></article></div><div className="split-grid"><article className="panel"><div className="panel-title"><h2>Sales pulse</h2><span>Last 7 days</span></div><div className="bars">{[42,65,52,78,61,88,72].map((height,i)=><div className="bar-wrap" key={i}><div className="bar" style={{height:`${height}%`}}/><small>{['M','T','W','T','F','S','S'][i]}</small></div>)}</div></article><article className="panel"><div className="panel-title"><h2>Quick status</h2><span>Live</span></div><div className="status-list"><p><i className="dot cyan"/>Inventory sync <b>Healthy</b></p><p><i className="dot gold"/>Orders awaiting review <b>7</b></p><p><i className="dot green"/>Staff online <b>4 / 6</b></p></div></article></div></section>}
